Package text_engine_smoke {
    let version = "0.1.0"
    let kira = "0.1.0"
    let kind = PackageKind.App
    let defaults = Defaults { executionMode: Backend.Vm, buildTarget: BuildTarget.Host }
    let dependencies = [
        Dependency { name: "KiraUIFoundation", path: "../.." }
    ]
}
