import KiraUIFoundation

struct TextBox {
    var label: String = ""
}

function readLabel() -> String {
    if KiraUIFoundation.stateSlotHas("smoke.text") == false {
        KiraUIFoundation.stateSlotPut("smoke.text", nativeUserData(nativeState(TextBox { label: "persist" })))
    }
    let box = nativeRecover<TextBox>(KiraUIFoundation.stateSlotGet("smoke.text"))
    return box.label
}

@Main
function main() {
    let first = readLabel()
    let second = readLabel()
    if first == "persist" {
        print("first-read-ok")
    }
    if second == "persist" {
        print("second-read-ok")
    }
}
